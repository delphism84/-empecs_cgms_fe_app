/.dart_tool/
/.idea/
/build/
/.vscode/
!/pubspec.lock
!/.packages
!/.flutter-plugins-dependencies
!/.flutter-plugins

# req-analyzer (local secrets / outputs)
/tools/req-analyzer/req-analyzer.config.local.jsonc
/tools/req-analyzer/req-analyzer.config.local.json
/req/**/_analysis/

# Node / tooling
**/node_modules/
/tools/lang-sheet/lang.import.csv

# 민감 정보 (OAuth·메일 등)
/docs/cgms info.csv
/backend/config.json
/backend/logs/

# Flutter
*.iml

# Gradle 빌드 캐시(리포트 폴더에 딸려 들어옴)
**/.gradle/

# 대용량 산출물 — 레포에 담지 않는다(업로드 비용 대비 이득이 없음).
# 원본은 로컬/공유 드라이브에 보관하고, 메일·전달용으로만 사용한다.
#   docs/req260716/  : 요구문서 원본 docx·pptx (약 17MB)
#   report/qa_*/     : QA 실행 산출물 — 화면 캡처·로그 (회차당 수 MB)
/docs/req260716/
/report/qa_*/
tools/lang-sheet/lang.user_original_backup.xlsx
